# 思思行程安排 —— 开发文档

## 1. 项目目标

开发一个轻量级的单页 Web 应用，用于规划“一天之内”的行程时间。

核心使用方式：

用户在一条横向时间线上创建若干节点。

每两个节点之间填写预计耗时，例如：

- 20min
- 45min
- 1h
- 1h20min
- 2h05min

用户只需要给其中一个或多个节点输入已知的“到达时间 / 出发时间”，例如：

09:45

系统根据节点之间填写的耗时，自动向前或向后推算其他节点的时间。

整个行程以一条蓝色横线展示。

节点上限：20 个。

这是一个“时间换算 + 行程可视化”工具，不需要地图、路线导航、账号系统、数据库或复杂旅行功能。


---

# 2. 技术要求

优先实现为简单的纯前端单页应用。

建议技术栈：

- HTML
- CSS
- JavaScript / TypeScript

可以使用 React + Vite，但不是必须。

如果使用 React：

- React
- TypeScript
- Vite

不要引入大型 UI 框架。

不要使用后端。

不要依赖服务器。

应用应该可以部署到：

- GitHub Pages
- Vercel
- Netlify

也应该能够在本地直接运行。


---

# 3. 核心界面

页面主体是一条从左到右延伸的时间线。

示意：

09:00                09:45                    10:15
出发                  到达                     出发
 ●────────45min────────●────────30min──────────●
九龙塘                福田站                   商场


时间线颜色：

#2563EB

或使用接近 Tailwind blue-600 的蓝色。

节点圆点同样使用蓝色。


---

# 4. 页面布局

这是个人使用的行程工具，不是商业软件。界面保持简单，打开页面直接进入行程编辑。

- 网页标题和页面主标题统一使用「思思行程安排」。
- 主标题放在顶部工具栏内，不单独设置品牌导航栏或大幅标题区。
- 不显示品牌 Logo、英文品牌名、宣传标语、营销文案或 DEMO 标签。
- 不设置产品介绍、三步使用介绍区、宣传页脚或装饰性文案。
- 保留与操作有关的提示、时间图例、冲突提示及行程时间汇总。

页面分为两个主要区域：

## 顶部工具栏

包含：

「添加节点」

「重置」

可以显示：

节点数量：5 / 20


## 时间线区域

时间线必须水平排列。

结构：

节点 → 时间段 → 节点 → 时间段 → 节点

例如：

   09:00                      09:45                      10:15
   出发                       到达                       出发
    ●──────────45min──────────●──────────30min──────────●
  九龙塘                    福田站                     商场


时间线必须保持横向。

不要因为屏幕宽度不足自动换成纵向。

如果节点数量较多：

允许整个时间线区域横向滚动。


---

# 5. 节点 Node

最多允许：

20 个节点。

每个节点包含以下信息：

```ts
interface TimelineNode {
  id: string;
  name: string;
  type: "departure" | "arrival";
  manualTime: string | null;
  calculatedTime: string | null;
}
```


# 6. 个人网站发布与手机使用

- GitHub 仓库：`git@github.com:Jackyxia-hkbu/one_day_timeline.git`。
- 使用 GitHub Pages 发布纯静态网页，不需要后端。
- 手机浏览器必须可用；窄屏工具栏允许换行，按钮便于触摸。
- 时间线始终横向排列，在其自身区域内左右滑动，不让整页横向溢出。
- 输入框在手机端使用至少 16px 字体，避免输入时自动放大。
- 不加入商业宣传或账号系统。
